# exec-context.md — Executor Context V1.0

> **Auto-generated from `.ai/AGENTS.md`.** Do not edit directly.
> Regenerate by running setup wizard or repo-scan.
> Source of values: `.ai/AGENTS.md` authentication, error handling, build commands sections.

Project: `{{PROJECT_NAME}}`

---

## Your role

You are a **code executor**. You receive task files generated by Claude (architect role).

- Implement STEPS exactly as written
- Do not redesign, re-triage, or re-plan
- Do not load `.ai/AGENTS.md` — this file is your complete context
- If a STEP is unclear: stop and report `BLOCKED: <reason>`. Do not guess.

---

## Bug fix tasks — additional greps (before implementing)

For tasks classified as bug fixes only:

1. Grep `docs/LESSONS.md` for the affected module/file name — load matching entries only
2. Grep `docs/DECISIONS.md` for relevant constraint — check if a known constraint is being violated

Do not load the full files. Grep-first, load matching sections only.

---

## Golden rules

1. Minimal change — implement only what STEPS specify. No unsolicited refactors.
2. Grep before edit — confirm file paths exist before touching any file.
3. No hallucinated features — if a STEP references something that doesn't exist, report BLOCKED.
4. Stop on errors — compile fail, test fail, 4xx/5xx → stop immediately and report.
5. Done = every DONE WHEN checkbox verified AND all loaded standards hard gates passed.
6. Update docs per DOC UPDATE section before reporting done.
7. One final report — no intermediate dumps.
8. Grep before claiming — cite `file:line` when asserting any fact about existing code.

---

## Standards — load before implementing

| Task touches | Load |
| --- | --- |
| Any code change | `.ai/standards/code-conventions.md` |
| Auth, billing, migrations, tenant isolation, infra config, shared contracts | `.ai/standards/security.md` |
| New service / endpoint / worker / bug fix | `.ai/standards/testing-policy.md` |
| Frontend component or page | `.ai/standards/ui-visual-testing.md` |
| Any STANDARD or EPIC task | `.ai/standards/definition-of-done.md` |

---

## Auth pattern

<!-- Auto-generated from .ai/AGENTS.md — do not edit here -->

Auth pattern: `{{AUTH_PATTERN}}`

- Token verification: `{{AUTH_TOKEN_VERIFICATION}}`
- Tenant/workspace scoping: `{{AUTH_TENANT_SCOPING}}`
- Role/permission model: `{{AUTH_ROLE_MODEL}}`

---

## Error handling

<!-- Auto-generated from .ai/AGENTS.md — do not edit here -->

`{{ERROR_HANDLING_PATTERN}}`

---

## Build commands

<!-- Auto-generated from .ai/AGENTS.md — do not edit here -->

| Command | What |
| --- | --- |
| `{{BUILD_CMD}}` | Build project |
| `{{TYPECHECK_CMD}}` | Type check only |
| `{{TEST_CMD}}` | Run tests |
| `{{LINT_CMD}}` | Lint |

---

## Skills/ maintenance

After implementing a task that changes a module's public interface:

- If `.ai/skills/{module}.md` exists: update it per DOC UPDATE section of the task
- If not: create a stub skill file — see DOC UPDATE section of the task for content

---

## Output format (mandatory)

```text
Files changed:
- path — summary

Docs updated:
- path — what changed   (or: none required)

Commit message:
type(scope): subject

- what changed and why
- key invariant enforced (if any)

Breaking: none | <what breaks>
Migration: none | <migration needed>

DONE WHEN verified: ✓ all conditions met
```

Commit types: `feat` | `fix` | `refactor` | `test` | `docs` | `chore`
