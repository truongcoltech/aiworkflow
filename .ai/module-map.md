# Module Map
# Map human phrases → kebab-case module names
# Module name = folder under .ai/tasks/<module>/ and .ai/skills/<module>.md
# Fill during repo-scan or as modules are discovered during work.

# Format:
# <human phrase> -> <kebab-case-name>

# Examples (replace with your project's modules):
# auth -> auth
# authentication -> auth
# user profile -> users
# database -> db
# api -> api

# Catch-all
default -> misc
